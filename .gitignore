# Folder view configuration files
.DS_Store
Desktop.ini

# Thumbnail cache files
._*
Thumbs.db

# Files that might appear on external disks
.Spotlight-V100
.Trashes

# Compiled Python files
*.pyc

# Compiled C++ files
*.out

# Application specific files
venv
node_modules
.sass-cache

# images
head-source/

# Was fetched from the CurriculumVitae repo's "latest" release at deploy time.
# That repo is private now and the page no longer links a CV — kept ignored so a
# stray local copy can't be committed.
Jonathan-Funke-Kaiser-CV.pdf

# Local editor / agent config (preview launch.json, etc.)
.claude/